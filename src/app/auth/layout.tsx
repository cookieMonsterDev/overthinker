import styles from './auth.module.scss'

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <body> 
      <h1>sfoajesd;d</h1>
      {children}
    </body>
  )
}